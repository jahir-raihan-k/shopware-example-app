### This is an example app to setup & understand shopware extension development processes.
Only copy the `MyExampleApp` folder and place it in `custom` > `apps` folder of shopware local installation.

